  /* ONE CUBE */

  // const geometrySphere = new THREE.SphereGeometry(2.3, 54, 54); // these are dynamically added per artist  
  // const textureLoaderSphere = new THREE.TextureLoader();
  // const planetImage = '/assets/planet-red-texture.jpeg';
  // const textureSphere = textureLoaderSphere.load(planetImage);

  // const materialSphere = new THREE.MeshStandardMaterial( { map:  textureSphere } );  // These are the images from artist
  // let sphere = new THREE.Mesh(geometrySphere, materialSphere);
  // sphere.position.z =+ -3;

  // this.scene.add(sphere);

